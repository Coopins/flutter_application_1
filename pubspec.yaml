name: flutter_application_1
description: "A new Flutter project."
publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ^3.7.2

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8

  # Core
  firebase_core: any
  firebase_auth: any
  cloud_firestore: any

  # Speech & audio
  speech_to_text: any
  flutter_tts: any
  record: any
  permission_handler: any

  # UI/markdown
  flutter_markdown: any

  # Utils
  http: any
  path_provider: any
  path: any
  flutter_dotenv: any

dev_dependencies:
  flutter_test:
    sdk: flutter
  # Keep v5 to avoid the Flutter SDK bump suggestion
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  assets:
    - .env
