# -----------------------------
# General / OS junk
# -----------------------------
*.class
*.log
*.pyc
*.swp
.DS_Store
.atom/
.build/
.buildlog/
.history
.svn/
.swiftpm/
migrate_working_dir/

# -----------------------------
# Environment files (do not commit secrets)
# -----------------------------
# Root .env patterns
.env
.env.*
*.env
!.env.sample            # allow a sample at root if you ever add one

# Env folder: keep real secrets out of git, allow example file
env/.env
env/.env.*
!env/.env.example

# -----------------------------
# IDE / Project files
# -----------------------------
*.iml
*.ipr
*.iws
.idea/
# Keep .vscode by default; uncomment to ignore the whole folder
# .vscode/

# -----------------------------
# Flutter / Dart / Pub
# -----------------------------
**/doc/api/
**/ios/Flutter/.last_build_id
.dart_tool/
.flutter-plugins
.flutter-plugins-dependencies
.pub-cache/
.pub/
.build/
build/
coverage/

# -----------------------------
# Android
# -----------------------------
/android/app/debug
/android/app/profile
/android/app/release
android/.gradle/
**/.gradle/

# Firebase Android config (secret)
android/app/google-services.json

# -----------------------------
# iOS
# -----------------------------
ios/.generated/
ios/.symlinks/
ios/Flutter/ephemeral/
ios/Flutter/Generated.xcconfig
**/xcuserdata/
**/*.xcuserstate

# Firebase iOS config (secret)
ios/Runner/GoogleService-Info.plist

# -----------------------------
# macOS
# -----------------------------
macos/.generated/
macos/.symlinks/
macos/Flutter/ephemeral/
**/xcuserdata/
**/*.xcuserstate

# -----------------------------
# Windows / Linux (build handled by /build ignore)
# -----------------------------
# (no extra ignores needed)

# -----------------------------
# Symbolication / Obfuscation
# -----------------------------
app.*.symbols
app.*.map.json
